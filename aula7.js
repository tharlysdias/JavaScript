var i = 0;
var j = 100;

for (i = 0; i < j; i++) {
    // pode ser usado no banco de dados
    // if (pessoa == joao)
    if (i == 10) {
        continue;
    }
    console.log("O valor atual de i é: " + i);
}